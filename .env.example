# Copy this file to .env and replace the password before starting the stack.
MYSQL_ROOT_PASSWORD=replace-with-a-strong-password

# Host ports. Change these if they are occupied.
BACKEND_PORT=8080
PORTAL_PORT=3000
ADMIN_PORT=3001
MONITOR_PORT=3002
