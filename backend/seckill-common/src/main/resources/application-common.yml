spring:
  datasource:
    username: root
    password: liao2004  # 数据库密码（可修改为自己的密码）
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      password: 123456  # Redis密码（可修改为自己的密码）

jwt:
  secret: SeckillCore2024SecretKeyForJWTTokenGenerationMustBeLongEnough
  expiration: 7200000  # Token 过期时间（毫秒），默认 2 小时